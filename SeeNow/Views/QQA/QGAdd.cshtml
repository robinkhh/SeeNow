@model IEnumerable<SeeNow.Models.quiz_group>

@{
    ViewBag.Title = "新增遊戲題組";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<header class="masthead form ">

    <div class="col-8 align-content-center offset-2">
        <h2>新增遊戲題組</h2>
    </div>

    <form method="post" action="@Url.Action("QGAdd")">
        @Html.AntiForgeryToken()

        <div class="container align-content-center">

            @*<h5>
                @Html.ActionLink("返回遊戲題組列表", "Index", "quiz_group")
            </h5>*@

            <div class="form-horizontal">
                <div class="form-group">
                    <label class="col-md-2 control-label" for="category_id">題庫類別名稱</label>
                    <div class="col-md-10">

                        @*<select id="category_id" name="category_id" class="form-control">
                            @foreach (var item in Model)
                            {
                                <option value="@item.category.category_id">@item.category.category_desc</option>
                            }
                        </select>*@
                        @Html.DropDownList("category",null,htmlAttributes:new { @class="form-control"})
                    </div>
                </div>

                @*<div class="form-group">
                    <label class="col-md-2 control-label" for="quiz_group1">遊戲題組編號</label>
                    <div class="col-md-10">
                        <input id="quiz_group1" name="quiz_group1" type="text" class="form-control" />
                    </div>
                </div>*@
                <div class="form-group">
                    <label class="col-md-2 control-label" for="group_name">遊戲題組名稱</label>
                    <div class="col-md-10">
                        <input id="group_name" name="group_name" type="text" class="form-control" />
                        <span class="text-danger">@ViewBag.Message</span>
                    </div>
                </div>

                <div class="form-group">
                    <label class="col-md-2 control-label" for="account">老師帳號</label>
                    <div class="col-md-10">
                        <input id="account" name="account" type="text" class="form-control" value="@Session["user_id"]" readonly="readonly"/>

                        @*<select id="account" name="account" class="form-control">
                            @foreach (var item in Model)
                            {
                                <option value="@item.users.account">@item.users.account</option>
                            }
                        </select>*@
                    </div>
                </div>

                <div class="form-group">

                    <div class="col-md-10 col-md-offset-2">
                        <input id="Submit1" type="submit" value="新增" class="btn btn-danger" />
                        <input id="Button1" type="button" value="取消" class="btn btn-info" onclick="location.href='@Url.Action("Index","quiz_group")'" />
                    </div>
                </div>

            </div>

        </div>
    </form>

</header>
