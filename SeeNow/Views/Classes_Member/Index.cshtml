@model IEnumerable<SeeNow.Models.classes_member>

@{
    ViewBag.Title = "班級成員列表";
}

<h2>班級成員列表</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table table-striped table-secondary table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.datetime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.classes.teacher_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.users.role_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.datetime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.classes.teacher_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.users.role_id)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>*@
    </tr>
}

</table>
